# CLAUDE.md

Read **[AGENTS.md](./AGENTS.md)** first — it defines the architecture, the machine-enforced layer rule,
the dependency-injection pattern, how to add a feature, and the Definition of Done. Everything else
defers to it.

Quick start: `pnpm install` → `pnpm dev` → run `pnpm check` before finishing any task.
